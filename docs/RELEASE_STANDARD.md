# RELEASE_STANDARD

## Release Workflow

The release workflow is implementation, self-validation, screenshot capture, report update, commit, push to `main`, GitHub visibility confirmation, and GitHub Pages confirmation.

Sprint reports must list changed files, validation commands, known limitations, and deployment status.

Future Codex prompts should start by reading `PROJECT_SPEC.md`, which links to these supporting standards.
